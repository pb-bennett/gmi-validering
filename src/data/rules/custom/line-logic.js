// src/data/rules/custom/line-logic.js
/**
 * Custom validation logic for Line objects.
 * Export functions that take (value, record) and return error string or null.
 */
export const customLineRules = {
  // Example:
  // "Dimensjon": (value, record) => {
  //   if (record.attributes.Material === "PE" && value < 32) return "PE pipes must be at least 32mm";
  //   return null;
  // }
};
