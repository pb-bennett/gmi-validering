// src/data/rules/custom/point-logic.js
/**
 * Custom validation logic for Point objects.
 * Export functions that take (value, record) and return error string or null.
 */
export const customPointRules = {
  // Example:
  // "Kumform": (value, record) => {
  //   if (record.attributes.OBJTYPE === "Kum" && !value) return "Kumform is required for Kum objects";
  //   return null;
  // }
};
