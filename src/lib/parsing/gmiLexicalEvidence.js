/**
 * Private per-attributes metadata for validation-authoritative GMI lexemes.
 * A symbol keeps the evidence out of ordinary field enumeration and JSON.
 */
export const GMI_SOURCE_LEXEMES = Symbol('gmiSourceLexemes');
